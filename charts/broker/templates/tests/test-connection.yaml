apiVersion: v1
kind: Pod
metadata:
  name: "{{ include "broker.fullname" . }}-test-connection"
  labels:
    {{- include "broker.labels" . | nindent 4 }}
  annotations:
    "helm.sh/hook": test
spec:
  containers:
    - name: wget
      image: busybox:1.36
      command: ['wget']
      args: ['--spider', '--timeout=5', 'http://{{ include "broker.fullname" . }}:{{ .Values.config.httpPort }}/healthz']
  restartPolicy: Never
